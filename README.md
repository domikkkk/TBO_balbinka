# Projekt `Balbinka`

Here is my open source contribution


to attack:
create a customer where name is {% set f = ''.format %}
and city is {{ f | my_filter(payload) }} (where payload is any text) that text should be called